[project]
name = "nexttoken"
version = "0.1.0"
description = "Interactive next-token visualization demo using Ollama"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "requests>=2.31",
]

[project.scripts]
demo = "demo:main"
